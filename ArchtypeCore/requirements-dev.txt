# Development and CI tooling, kept out of requirements.txt so the runtime image the
# Dockerfile builds never carries a linter, a type checker, or a test runner.
-r requirements.txt

pytest
pytest-asyncio
# The suite boots the application through Starlette's in-process test client, which speaks
# through httpx2.
httpx2
ruff
mypy
import-linter
