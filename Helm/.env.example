# Copy to .env.local and adjust. Every variable the app consumes is mirrored here.
# "mock" serves the in-browser demo API; "live" targets a real backend at VITE_API_BASE_URL.
VITE_API_MODE=mock
VITE_API_BASE_URL=/api
